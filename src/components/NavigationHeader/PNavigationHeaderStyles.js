import { LARGE } from '@styles/styles';

const styles = {
  container: {
    alignItems: 'center',
    justifyContent: 'center',
    flexDirection: 'row',
    
    minHeight: 40,
    paddingTop: 20
  },
  defaultIcon: {
    fontSize: 30
  },
  defaultTextContainer: {
    flex: 1,
    alignItems: 'center',
    paddingRight: 30
  },
  defaultText: {
    fontSize: LARGE 
  }
};

export default styles;
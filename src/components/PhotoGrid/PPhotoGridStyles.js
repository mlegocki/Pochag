import { width } from '@styles/styles';

const styles = {
  defaultContainer: {
    flex: 1,
    flexDirection: 'row',
    flexWrap: 'wrap',
  },
  defaultImageContainer: { 
    width: width / 3 - 4,
    height: width / 3 - 4,
    margin: 2,
  },
  defaultImage: {
    flex: 1
  }
};

export default styles;